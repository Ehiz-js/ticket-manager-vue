<script setup>
import { useRouter } from 'vue-router'

const router = useRouter()

const goBack = () => {
  router.push('/')
}
</script>

<template>
  <button class="backButton" @click="goBack">&#8592;</button>
</template>

<style scoped>
.backButton {
  position: absolute;
  top: 1rem;
  left: 1rem;
  background-color: var(--color-primary);
  border: none;
  color: white;
  font-size: 2rem;
  font-weight: 500;
  cursor: pointer;
  padding: 0.4rem 0.75rem;
  padding-bottom: 0.8rem;
  border-radius: 50%;
  transition:
    background 0.2s,
    color 0.2s;
}

.backButton:hover {
  background-color: rgba(0, 112, 243, 0.1);
  color: #005bb5;
}

/* Mobile responsiveness */
@media (max-width: 480px) {
  .backButton {
    font-size: 0.9rem;
    padding: 0.3rem 0.6rem;
    padding-bottom: 0.65rem;
    top: 0.75rem;
    left: 0.75rem;
  }
}
</style>
